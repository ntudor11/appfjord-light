# Claude Code Skills

This folder is intentionally empty for now.

## What to do if you are Claude Code reading this

Load a skill only when the current task matches its domain (progressive
disclosure — do not pre-load every skill into context). If the skill you need
does not exist yet in `.agents/skills/`, fall back to
[AGENTS.md](../../AGENTS.md) and [.agents/context/](../../.agents/context/)
for general conventions instead of guessing domain-specific procedure.
